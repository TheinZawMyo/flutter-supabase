import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color gradientStart = Color(0xFF6A11CB);
  static const Color gradientEnd = Color(0xFF2575FC);

  static const Color primaryText = Colors.white;
  static const Color secondaryText = Colors.white70;
  static const Color divider = Colors.white38;

  static final Color inputFieldBackground = Colors.white.withValues(alpha: 0.1);
  static const Color buttonForeground = Colors.white;
  static const Color buttonBackground = Color(0xFF2575FC); // Vibrant Blue
  static const Color accentButtonBackground = Color(
    0xFF6A11CB,
  ); // Vibrant Purple
}
